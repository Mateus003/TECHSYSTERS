<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Test" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorPrimary">#A226C5</item>
        <item name="colorPrimaryVariant">#A226C5</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Test.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Test.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Test.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DatePickerStyle" parent="@android:style/Widget.Material.DatePicker">
        <item name="android:headerBackground">@color/color_default</item>
        <item name="android:calendarTextColor">@color/color_default</item>
        <item name="android:dayOfWeekBackground">@color/color_default</item>
        <item name="android:dayOfWeekTextAppearance">@style/YourDayOfWeekTextAppearance</item>
        <item name="android:dateTextAppearance">@style/YourDateTextAppearance</item>
    </style>

    <style name="YourDayOfWeekTextAppearance" parent="@android:style/TextAppearance.Material.Small">
        <item name="android:textColor">@color/color_default</item>
    </style>

    <style name="YourDateTextAppearance" parent="@android:style/TextAppearance.Material.Medium">
        <item name="android:textColor">@color/color_default</item>
    </style>

</resources>
